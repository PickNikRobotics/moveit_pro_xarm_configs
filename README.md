# moveit_pro_xarm_configs

PickNik-provided configuration for using xarm robots with MoveIt Pro.  Provides everything you need out of the box to incorporate an xarm robot into your MoveIt Pro configuration.

Provided configurations:
* base_config - a basic xarm config including mock hardware simulation
* sim_config - a basic xarm config that includes MoveIt Pro's high fidelity physics simulator
